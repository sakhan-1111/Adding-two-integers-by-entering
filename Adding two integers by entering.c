#include <stdio.h>
#include <conio.h>

int main()
{
    int x,y,sum;
    printf("Enter the first number = ");
    scanf("%d", &x);
    printf("Enter the second number = ");
    scanf("%d", &y);
    sum=x+y;
    printf("The result is = %d",sum);
    return 0;
}
